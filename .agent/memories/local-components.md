# Local Component Index

> Auto-generated from `markup/components/**/*.vue` — 211 files.
> Sorted alphabetically by File path.
> Props column lists `defineProps` fields only (types abbreviated).
> "Used In" is derived from grep of `markup/pages/**/*.vue`.

| Component | File path | Props | Purpose | Used In |
|-----------|-----------|-------|---------|---------|
| `ApplyList` | `markup/components/daily-mart/apply-list/ApplyList.vue` | — | Renders the daily-mart apply items list section | `daily-mart/index.vue`, `daily-mart/Pc.vue` |
